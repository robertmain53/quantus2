1. "This is a simple_calc because calculating gross margin requires multiple inputs (e.g., revenue, cost of goods sold, allowances, and optional adjustments) and produces one primary output (gross margin percentage) plus useful secondary outputs (gross profit, markup, margin ratio)."

2. "I will use `component_type: 'simple_calc'` with `logic.type: 'formula'`."

3. "I will structure `page_content` as flat arrays only: introduction[], methodology[], faqs[], citations[]."

4. "All URLs in citations will be plain HTTPS strings, no Markdown like `[url](url)`, no HTML."

5. "I will NOT add any forbidden fields (no slug, name, category, meta, schema_org, canonical_url, robots, keywords, styling, or form for converters, etc.)."

6. "I understand this is the final JSON and I cannot add commentary or explanations afterward."

Ready for me to produce the JSON?