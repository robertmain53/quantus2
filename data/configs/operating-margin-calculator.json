1. This is a simple_calc because the contribution margin requires multiple related inputs (e.g., selling price, variable cost per unit, units sold, fixed costs) to compute one or more formulaic outputs (contribution margin per unit, contribution margin ratio, total contribution, and break-even units).

2. I will use component_type: "simple_calc" with logic.type: "formula".

3. I will structure page_content as flat arrays only: introduction[], methodology[], faqs[], citations[] (and optional examples/summary/glossary as arrays of strings).

4. All URLs in citations will be plain HTTPS strings, no Markdown or HTML.

5. I will NOT add any forbidden fields (no extra metadata fields, no UI/layout/help keys, no form for converters, etc.).

6. I understand this is the final JSON and I cannot add commentary or explanations afterward.

Ready for me to produce the JSON?