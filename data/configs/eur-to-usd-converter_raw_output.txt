1. This is a simple_calc because currency conversion requires a variable exchange rate or user-supplied rate (not a fixed mathematical constant). Users will input an amount (EUR) and either a current exchange rate or choose to use a fetched rate; the calculation uses a formula to compute one primary output (USD).

2. I will use component_type: "simple_calc" with logic.type: "formula".

3. I will structure page_content as flat arrays only: introduction[], methodology[], faqs[], citations[] (and optional examples/summary/glossary as flat arrays if included).

4. All URLs in citations will be plain HTTPS strings, no Markdown or HTML.

5. I will NOT add any forbidden fields (no canonical_url, robots, schema_org, open_graph, twitter_card, styling, or any extraneous keys). The converter-specific restriction (no form) will be respected — since this is a simple_calc, a form will be included as required by the schema.

6. I understand this is the final JSON and I cannot add commentary or explanations afterward.

I have scanned the provided input assets and will draw on authoritative sources for methodology and trust signals (NIST, ISO, U.S. government data, leading universities) and will reference related internal Cernarus pages for context (for example: /conversions/currency-basics, /conversions/fees, /conversions/historical-rates) while ensuring citations are plain HTTPS links.

Ready for me to produce the JSON?